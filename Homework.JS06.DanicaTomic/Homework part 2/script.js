// create a array of numbers
let numbers = [2, 4, 5];
// print all numbers from the array in a list element in the html
function () {
  .innerHTML = "";
  .innerHTML += `<ul>`;
  for(let i=0; i < subjects.length; i++){
      element.innerHTML += `<li>${subjects[i]}: ${grades[i]}</li>`;
  }
  element.innerHTML += "</ul>"
}
// print sum of all numbers bellow the list
// try printing 2 + 4 + 5 = 11
